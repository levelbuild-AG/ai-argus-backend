apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "librechat-mt.fullname" . }}-env
  labels:
    {{- include "librechat-mt.labels" . | nindent 4 }}
data:
  MULTI_TENANCY_ENABLED: {{ .Values.env.MULTI_TENANCY_ENABLED | quote }}

