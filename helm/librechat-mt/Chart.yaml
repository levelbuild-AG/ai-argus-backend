apiVersion: v2
name: librechat-mt
description: MT-ready LibreChat backend (api + rag_api) with optional Knative workloads and pluggable backing services.
type: application
version: 0.1.0
appVersion: "1.0.0"

