# This file layers minimal overrides on top of the upstream defaults.
# Keep `use_default_settings: true` when upgrading to avoid schema drift.

use_default_settings:
  engines:
    keep_only:
      - google custom search

general:
  debug: false
  instance_name: "LibreChat SearXNG"
  search_on_category_select: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "CHANGE_ME_TO_RANDOM_LONG_STRING"

search:
  safe_search: 0
  autocomplete: "duckduckgo"
  formats:
    - html
    - json

engines:
  - name: google custom search
    engine: google_cs
    shortcut: gcs
    api_key: !env_var GOOGLE_SEARCH_API_KEY
    cx: !env_var GOOGLE_CSE_ID
    categories:
      - general
      - web
      - news
    timeout: 3.0
    disabled: false

ui:
  default_theme: simple