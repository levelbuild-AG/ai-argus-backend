# DNS A record setup
```bash
Type: A
Name: <tenant>-argus-chat.levelbuild.com
Value: External IP of VM
TTL: Default
```

# Setting up an External IP
Go to 

# Running setup_vm.sh
Open a Git Bash Terminal
and run:
```bash
cd ai-argus-backend/
chmod +x scripts/setup_vm.sh
./scripts/setup_vm.sh
```